# Archived configuration snippets

This directory stores old configuration snippets that are useful as reference material but
are not imported by any host.

- `nixvim/old-default.nix`
- `nixvim/old-relief.nix`

Active NixVim configuration belongs under `modules/home/nixvim/`.
